<div class="container pt-4 py-3">
    <div class="row m-1">
        <h2>订单详情</h2>
    </div>
    <div class="row text-secondary ml-1">
        <small>如有疑问，请及时咨询客服！</small>
    </div>
</div>

<div class="container py-2">
    <div class="row m-1 py-2">
        <h5>订单状态</h5>
    </div>

    <div class="row no-gutters bg-white p-3 mr-2 d-flex flex-column border rounded overflow-hidden shadow-sm">
        <h3 class="m-2 text-center">{{typeStatusReadable}}</h3>
        {{#if deliverCode}}
            <small class="d-inline-block text-secondary text-center">快递单号：{{deliverCode}}</small>
        {{/if}}
    </div>
</div>

<div class="container py-2">
    <div class="row m-1">
        <h5>收货地址</h5>
    </div>
</div>

<div class="container">
    <div class="row ml-2">
        <div class="border-success col-3 bg-white p-3 mx-2 d-flex flex-column border rounded overflow-hidden shadow-sm">
            <p class="mb-1">{{address.name}} {{address.phone}}</p>
            <small class="mb-0">{{address.area}}</small>
            <small class="mb-0">{{address.detailAddress}}</small>
        </div>
    </div>
</div>

<div class="container pt-4">
    <div class="row ml-2">
        <h5>订单信息</h5>
    </div>
</div>

<div class="container py-2">

    <div class="row bg-white no-gutters border rounded overflow-hidden flex-md-row py-2 mb-3 shadow-sm h-md-250 position-relative">
        <div class="col-2 d-flex flex-column position-static">
            <small class="my-auto px-3">商品图片</small>
        </div>

        <div class="col-7 d-flex flex-column position-static">
            <small class="my-auto">商品名称</small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto">单价</small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto">数量</small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto">总价</small>
        </div>
    </div>

    {{#each products}}
    <div class="row bg-white no-gutters border rounded overflow-hidden flex-md-row mb-3 shadow-sm h-md-250 position-relative">
        <div class="col-2 d-flex flex-column position-static">
            <div class="container my-auto pl-4 py-4">
                <img class="border rounded overflow-hidden" width="80" height="80" src="{{product.thumbnail.url}}" alt="商品描述">
            </div>
        </div>

        <div class="col-7 d-flex flex-column position-static">
            <small class="my-auto">{{product.name}}</small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto"><b>¥ {{product.price}}</b></small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto">{{count}}</small>
        </div>

        <div class="col d-flex flex-column position-static">
            <small class="my-auto text-danger"><b>¥ {{price}}</b></small>
        </div>
    </div>
    {{/each}}

    <div class="row bg-white no-gutters border rounded overflow-hidden flex-md-row mb-3 p-3 shadow-sm h-md-250 position-relative">
        <div class="container">
            <p class="mb-0"><small>订单号：{{id}}</small></p>
            <p class="mb-0"><small>运单号：{{deliverCode}}</small></p>
            <p class="mb-0"><small>创建时间：{{createDateTimeReadable}}</small></p>
            <p class="mb-0"><small>付款时间：{{payTimeReadable}}</small></p>
            <p class="mb-0"><small>发货时间：{{deliverTimeReadable}}</small></p>
            <p class="mb-0"><small>评价时间：{{finishTimeReadable}}</small></p>
            <p class="mb-0"><small>退款时间：{{refundTimeReadable}}</small></p>
        </div>
    </div>
</div>